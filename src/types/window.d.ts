// na sessionStorage "sb-pp-tmp" (per-tab isolation, explicit cleanup, mniejszy attack vector
// dla XSS z CSP unsafe-inline). Type declaration USUNIETA — pole nie jest juz uzywane.
//
// File pozostaje (nie usuwamy) zeby zachowac slot dla przyszłych Window extensions.
export {};
